import { resolver } from 'graphql-sequelize';
import { Headquarter } from '../../models/headquarter.model';
import to from 'await-to-js';

export const HeadquarterMap = {
    //users: resolver(Headquarter.associations.users),
};